import React from 'react';
import style from './Card.css';

const Card = () => {
  return (
    <div className={`${style.item} ${style.footer}`}>
        <h2>Card</h2>
      </div>
  );
};

export default Card;